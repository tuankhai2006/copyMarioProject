#include "PlayerData.h"

PlayerData::PlayerData(int lives, int coins, int score)
{
    this->lives = lives;
    this->coins = coins;
    this->score = score;
    // Initialize other persistent data as needed
}